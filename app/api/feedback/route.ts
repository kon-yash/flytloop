import { z } from 'zod';import { feedbackList,createFeedback } from '@/lib/services/feedback';import { json,apiError } from '@/lib/api';import { ensureImported } from '@/lib/import/importAll';export const dynamic='force-dynamic';const Create=z.object({type:z.enum(['FEATURE_REQUEST','BUG','SUPPORT']),title:z.string().min(3).max(200),summary:z.string().optional(),productArea:z.string().optional(),priority:z.enum(['LOW','MEDIUM','HIGH','CRITICAL']).optional(),ownerTeam:z.enum(['PRODUCT','ENGINEERING','SOLUTIONS']).optional(),accountIds:z.array(z.string()).optional(),parentId:z.string().optional()});export function GET(){try{ensureImported();return json(feedbackList())}catch(e){return apiError(e)}}export async function POST(request:Request){try{return json(createFeedback(Create.parse(await request.json())),201)}catch(e){return apiError(e)}}
