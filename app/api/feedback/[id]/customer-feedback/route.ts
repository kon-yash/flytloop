import { z } from 'zod';import{addCustomerFeedback,createFeedback}from'@/lib/services/feedback';import{closeFeedback}from'@/lib/ai/provider';import{json,apiError}from'@/lib/api';const Input=z.object({accountId:z.string(),text:z.string().min(3),createLinked:z.boolean().optional()});export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,i=Input.parse(await r.json()),analysis=closeFeedback(i.text);addCustomerFeedback(id,{...i,sentiment:analysis.sentiment,analysis});const linked=i.createLinked&&analysis.newRequirementDetected?createFeedback({type:'FEATURE_REQUEST',title:analysis.newRequirementTitle!,summary:analysis.newRequirementSummary!,productArea:'missions',accountIds:[i.accountId],parentId:id}):null;return json({analysis,linked})}catch(e){return apiError(e)}}
