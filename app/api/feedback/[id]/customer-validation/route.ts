import { z } from 'zod';import{addValidation}from'@/lib/services/feedback';import{json,apiError}from'@/lib/api';const Input=z.object({accountId:z.string(),demoGiven:z.boolean(),customerTried:z.enum(['YES','NO','NOT_YET']),reaction:z.enum(['POSITIVE','NEUTRAL','NEGATIVE']).optional()});export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;return json(addValidation(id,Input.parse(await r.json())))}catch(e){return apiError(e)}}
