import{z}from'zod';import{db,now,audit}from'@/lib/db';import{json,apiError}from'@/lib/api';const Input=z.object({body:z.string().min(1).max(3000)});export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,i=Input.parse(await r.json());db().prepare('INSERT INTO feedback_comments(feedback_id,body,author,created_at) VALUES (?,?,?,?)').run(id,i.body,'Demo User',now());audit('feedback',id,'comment_added',i.body.slice(0,100));return json({ok:true})}catch(e){return apiError(e)}}
